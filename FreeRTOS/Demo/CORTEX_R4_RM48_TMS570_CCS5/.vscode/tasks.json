{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build FreeRTOS",
            "type": "shell",
            "command": "cmake --build build",
            "group": "build",
            "problemMatcher": ["$gcc"]
        }
    ]
}